const data = { count: 8, isValid: true, title: "Exam", temperature: 87.88 };

// Destructuring the value of 'title' to a local variable
const { title } = data;

// Now 'title' contains the value "Exam"
console.log(title); // Output: Exam
