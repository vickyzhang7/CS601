// Given object
const data = { count: 8, isValid: true, title: "Exam", temperature: 87.88 };

// Destructure the 'title' property into a local variable
const { title } = data;

// Use the 'title' variable
console.log(title); // Outputs: "Exam"

